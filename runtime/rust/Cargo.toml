[package]
name = "jarvis-runtime"
version = "0.1.0"
edition = "2021"

[lib]
name = "jarvis_runtime"
path = "src/lib.rs"

[dependencies]

[lints.rust]
unsafe_code = "forbid"
missing_debug_implementations = "warn"
missing_docs = "warn"
