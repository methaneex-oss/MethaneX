[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "methane-x"
version = "0.3.0"
description = "MethaneX JARVIS cognitive prototype"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["methane_x*"]
