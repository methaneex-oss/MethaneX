[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "methane-x"
version = "0.1.0"
description = "MethaneX JARVIS prototype"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["methane_x*"]
