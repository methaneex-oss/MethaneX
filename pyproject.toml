[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "jarvis"
version = "0.3.1"
description = "JARVIS cognitive assistant prototype"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["jarvis*", "methane_x*"]
